# خارطة العمل

- [x] تصفح نظام المدرسة الحقيقي (se.fiksutilitoimisto.fi) كإدارة وحصر جميع الأقسام والصفحات (40 قسم / 323 صفحة).
- [x] إضافة فهرس الأقسام داخل بوابة المدير `/m` وصفحة لكل قسم وصفحة لكل شاشة `/m/{قسم}/{صفحة}` بنفس هيكل تواصل (مسار، تصفيات، معاينة، جدول/حالة فارغة).
- [x] تحويل أقسام النظام إلى قوائم عربية متعددة المستويات داخل الشريط الجانبي مع فتح المسار الحالي تلقائياً.
- [x] استبدال صفحة اختيار البوابات بواجهة تسجيل دخول عربية موحدة وجاهزة للربط.
- [ ] تفصيل محتوى الصفحات ذات الأولوية (المستخدمون، الطلبة، الموظفون، الحضور، المالية، الجدول المدرسي) بجداول وحقول مطابقة.
- [ ] ربط تسجيل الدخول والصفحات بواجهة برمجة سيرفر المدرسة الحقيقي، مع ملفات المستخدمين والأدوار (بانتظار مفاتيح/عنوان الـ API من المستخدم).
